<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:core="clr-namespace:QuickPOS.Core"
             x:Class="QuickPOS.Features.Inventory.InventoryBatchView"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1100"
             Background="{StaticResource SurfaceBrush}">
    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Stats bento row -->
                <RowDefinition Height="*"/>
                <!-- Batch DataGrid -->
            </Grid.RowDefinitions>
           
            <!--  Batch DataGrid -->
            <Border Grid.Row="1" Background="{StaticResource SurfaceLowBrush}" CornerRadius="12">
                <DataGrid ItemsSource="{Binding Batches}" AutoGenerateColumns="False"
                      IsReadOnly="True" CanUserSortColumns="True"
                      HeadersVisibility="Column" GridLinesVisibility="None"
                      BorderThickness="0"
                      Background="Transparent"
                      RowBackground="{StaticResource SurfaceLowestBrush}"
                      AlternatingRowBackground="{StaticResource SurfaceLowBrush}"
                      FontFamily="Inter, Segoe UI" FontSize="13"
                      ColumnHeaderHeight="48" RowHeight="56"
                      SelectionMode="Single" SelectionUnit="FullRow">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource SurfaceHighBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource OnSurfaceVariantBrush}"/>
                            <Setter Property="FontFamily" Value="Inter"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="20,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>

                        <!-- Product name -->
                        <DataGridTemplateColumn Header="{Binding InvColProduct, Source={x:Static core:LocalizationService.Instance}}" Width="*" SortMemberPath="ProductName">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ProductName}"
                                               FontFamily="Inter, Segoe UI" FontWeight="Bold"
                                               Foreground="{StaticResource OnSurfaceBrush}"
                                               VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Received date -->
                        <DataGridTemplateColumn Header="{Binding InvColReceived, Source={x:Static core:LocalizationService.Instance}}" Width="140" SortMemberPath="ReceivedAt">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ReceivedAt, StringFormat=dd/MM/yyyy HH:mm}"
                                           FontFamily="Inter, Segoe UI" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Reference -->
                        <DataGridTemplateColumn Header="{Binding InvColReference, Source={x:Static core:LocalizationService.Instance}}" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock FontFamily="Inter, Segoe UI" FontSize="12"
                                           Foreground="{StaticResource OutlineBrush}"
                                           VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="{Binding Reference}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Reference}" Value="{x:Null}">
                                                        <Setter Property="Text" Value="--"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Unit Cost -->
                        <DataGridTemplateColumn Header="{Binding InvColUnitCost, Source={x:Static core:LocalizationService.Instance}}" Width="110" SortMemberPath="UnitCost">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding UnitCost, StringFormat={}{0:C2}}"
                                           FontFamily="Manrope, Segoe UI" FontWeight="SemiBold"
                                           Foreground="{StaticResource PrimaryDarkBrush}"
                                           VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- QTY In -->
                        <DataGridTemplateColumn Header="{Binding InvColQtyIn, Source={x:Static core:LocalizationService.Instance}}" Width="90" SortMemberPath="QuantityReceived">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding QuantityReceived}"
                                           FontFamily="Inter, Segoe UI"
                                           Foreground="{StaticResource OnSurfaceVariantBrush}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- QTY Sold -->
                        <DataGridTemplateColumn Header="{Binding InvColQtySold, Source={x:Static core:LocalizationService.Instance}}" Width="90" SortMemberPath="QuantitySold">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding QuantitySold}"
                                           FontFamily="Inter, Segoe UI"
                                           Foreground="{StaticResource TertiaryBrush}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- QTY Remaining + progress bar -->
                        <DataGridTemplateColumn Header="{Binding InvColRemaining, Source={x:Static core:LocalizationService.Instance}}" Width="140" SortMemberPath="QuantityRemaining">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock FontFamily="Inter, Segoe UI" FontWeight="Bold"
                                               HorizontalAlignment="Right" Margin="0,0,0,3">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="{Binding QuantityRemaining}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource OnSurfaceBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsExhausted}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                                            <Setter Property="Text" Value="{Binding InvDepleted, Source={x:Static core:LocalizationService.Instance}}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <ProgressBar Minimum="0" Maximum="{Binding QuantityReceived, Mode=OneWay}"
                                                 Value="{Binding QuantityRemaining, Mode=OneWay}"
                                                 Height="4" BorderThickness="0"
                                                 Background="{StaticResource SurfaceHighestBrush}"
                                                 Foreground="{StaticResource PrimaryBrush}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Remaining Value -->
                        <DataGridTemplateColumn Header="{Binding InvColLayerValue, Source={x:Static core:LocalizationService.Instance}}" Width="120" SortMemberPath="RemainingValue">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding RemainingValue, StringFormat={}{0:C2}}"
                                           FontFamily="Manrope, Segoe UI" FontWeight="Bold"
                                           Foreground="#0D9488"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>


        <!-- Floating Action Button: Receive Stock -->
        <Button Width="176" Height="64"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Margin="0,0,40,40"
                Cursor="Hand"
                Command="{Binding ShowReceiveFormCommand}"
                ToolTip="Receive Stock">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="fabBorder" Width="164" Height="44" CornerRadius="22"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
                                <GradientStop Color="{StaticResource PrimaryContainerColor}" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="24" Opacity="0.25" ShadowDepth="6"
                                              Color="{StaticResource PrimaryColor}"/>
                        </Border.Effect>
                        <Border.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Border.RenderTransform>
                        <TextBlock Text="{Binding InvReceiveStock, Source={x:Static core:LocalizationService.Instance}}" FontFamily="Manrope, Segoe UI" FontSize="18" FontWeight="Bold"
                                   Foreground="White"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Margin="0,-2,0,0"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="fabBorder" Property="Opacity" Value="0.4"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="fabBorder" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="fabBorder" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
    <!-- /outer Grid -->
</UserControl>